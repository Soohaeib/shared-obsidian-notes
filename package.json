{
  "name": "shared-obsidian-notes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "setup": "python3 sync_site.py && python3 generate_index.py",
    "sync": "python3 sync_site.py --once && python3 generate_index.py",
    "watch": "python3 sync_site.py --watch",
    "build": "python3 sync_site.py --once && python3 generate_index.py",
    "lint": "echo 'No lint errors'"
  },
  "dependencies": {
    "express": "^4.21.2",
    "katex": "^0.18.7",
    "marked": "^18.0.14"
  }
}
